public interface Builder {
    PCBuilder setRAMMemory(String RAMMemory);
    PCBuilder setDisk(String Disk);
    PCBuilder setGPU(String GPU);
    PCBuilder setCPU(String CPU);
    PCBuilder setMemory(String Memory);
    PCBuilder setName(String Name);
}
